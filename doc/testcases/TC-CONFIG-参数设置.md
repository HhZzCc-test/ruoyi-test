# 参数设置 - 测试用例

> 接口：GET /system/config/list
> 说明：参数设置模块，查询系统参数列表
> 模块：系统管理

---

## TC-CONFIG-001

| 项目 | 内容 |
|------|------|
| **用例ID** | TC-CONFIG-001 |
| **用例标题** | 正常场景 - 获取参数列表 |
| **优先级** | P0（Critical） |
| **前置条件** | 已登录获取有效Token |
| **测试类型** | 功能测试 - 正常场景 |

**断言清单：**
- HTTP状态码 = 200
- 业务码 code = 200
- 字段 rows 存在且为列表类型
- 字段 total 存在且为整数类型
- 响应时间 < 3s

---

## TC-CONFIG-002

| 项目 | 内容 |
|------|------|
| **用例ID** | TC-CONFIG-002 |
| **用例标题** | 权限场景 - 无Token访问参数列表 |
| **优先级** | P0（Critical） |
| **前置条件** | 无 |
| **测试类型** | 功能测试 - 权限场景 |

**预期结果：** code != 200，返回认证失败错误

---

## TC-CONFIG-003

| 项目 | 内容 |
|------|------|
| **用例ID** | TC-CONFIG-003 |
| **用例标题** | 权限场景 - 无效Token访问参数列表 |
| **优先级** | P1（Normal） |
| **前置条件** | 无 |
| **测试类型** | 功能测试 - 权限场景 |

**预期结果：** code != 200，返回认证失败错误

---

> 用例总数：3 条 | P0: 2 | P1: 1